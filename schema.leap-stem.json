{
  "$schema": "http://json-schema.org/draft-06/schema#",
  "title": "Chain of updates to data state, with leaps",

  "type": "object",
  "required": ["stemId"],
  "properties": {
    "stemId": { "$ref": "#/definitions/stemId" },
    "main": { "$ref": "#/definitions/stemId" },
  },
  "definitions": {
    "stemId": {
      "type": "string",
      "description": "Identifier for a unique data node, based on data state checksums"
    },
  }
}